# Postgres credentials — consumed by docker-compose.yml
POSTGRES_USER=paperweight
POSTGRES_PASSWORD=paperweight
POSTGRES_DB=paperweight
POSTGRES_PORT=5432

# The DSN the loader and the metadata tools connect with.
# Keep it in sync with the values above.
DATABASE_URL=postgresql://paperweight:paperweight@localhost:5432/paperweight
