psycopg[binary]>=3.2,<4
# macOS: faiss and torch each vendor libomp.dylib and segfault when both load.
# src/embeddings.py detects this and prints the one-line symlink fix.
faiss-cpu>=1.9
# Progress bars for the COPY phase (src/loader.py) and the embed phase
# (src/embeddings.py).
tqdm>=4.66
# Test runner.
pytest>=8
