fastmcp==4.0.3
httpx==0.28.1
pydantic==2.13.5
pydantic-settings==2.15.0
rapidfuzz==3.14.6
# The cross-encoder that reorders search candidates against the query in
# src/services/models/reranker.py, and downloads its weights on first use. Pulls
# in torch and transformers, which are most of the install's footprint (~2GB
# against the ~30MB of everything above); neither is imported directly, so
# neither is pinned here.
sentence-transformers==6.0.1
tenacity==9.1.4
# Unit tests for the research API clients (tests/); httpx.MockTransport, which
# stands in for the network there, ships with httpx itself.
pytest>=8
